# This is an example file. Copy this to a .env file for local development.
# IMPORTANT: Do not commit the .env file to version control.

# The port the backend server will run on
PORT=3001

# The URL of the frontend application for CORS
FRONTEND_URL=http://localhost:3000

# Firebase Admin SDK credentials
# This should be a JSON string, not a path to a file.
# It's recommended to use your hosting provider's secret management for this in production.
# Example: '{"type":"service_account",...}'
FIREBASE_ADMIN_SDK_JSON=""

# Stripe API keys
STRIPE_SECRET_KEY="your_stripe_secret_key_here"
STRIPE_WEBHOOK_SECRET="your_stripe_webhook_secret_here"

# Database connection URL
# Example for PostgreSQL: "postgresql://user:password@host:port/database"
DATABASE_URL=""
